import { MixedchartDirective } from './mixedchart.directive';

describe('MixedchartDirective', () => {
  it('should create an instance', () => {
    const directive = new MixedchartDirective();
    expect(directive).toBeTruthy();
  });
});
